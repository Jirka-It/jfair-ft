const context = "sasha-system";
const LOCAL_DATA =
{
  lang:           "lang",
  email:          "email" + context,
  language:       "language",
  password:       "password" + context,
  affiliate:      "affiliate" + context,
  removeItem:     "removeItem" + context,
  token_type:     "token_type" + context,
  currentUser:    "currentUser" + context,
  expire_date:    "expire_date" + context,
  access_token:   "access_token" + context,
  refresh_token:  "refresh_token" + context,
  additionalInfo: "additionalInfo" + context,
  rebember_value: "rebember_value" + context,
};
export default LOCAL_DATA;